const fileGit = require('./gitConfig');
const fileEditorConfig = require('./editorConfig');
const readmeConfig = require('./readme');

module.exports = {
  fileGit,
  fileEditorConfig,
  readmeConfig
}